export const APP = {
  map: {
    'mode': 'mode',
    'basemap': 'basemap',
    'viewType': 'viewType',
    'viewTypes': {
      'map': 'map',
      'scene': 'scene'
    }
  }
};

export const MAP = APP.map;
